# digiwf-test-processes


## Generate

```
npx @miragon/miragon-process-ide-cli generate -n <project-name> -p <path>
```

## Deploy

```
npx @miragon/miragon-process-ide-cli deploy-all -d <directory> -t <target-env> -p <project-name>
```
